{
  "name": "open-tts",
  "version": "1.0.0",
  "description": "Self-hosted text-to-speech and speech-to-text service with an OpenAI-compatible API",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check index.js && node --check server.mjs",
    "test": "NODE_ENV=test node --test 'test/**/*.test.mjs'"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "remove-markdown": "^0.7.0"
  }
}
